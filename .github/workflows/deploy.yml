name: Continuous Deployment


on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: akhileshns/heroku-deploy@v3.6.8 # This is the action
        with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY_V}}
          heroku_app_name: "test-covid-track"
          heroku_email: "nhoxchemminnoluudan@gmail.com"
